<h1>Users</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
  </tr>

<% @users.each do |user| %>
  <tr>
    <td><%= user.name %></td>
    <td><%= link_to 'Show', user %> |</td>
    <td><%= link_to 'View Posts', user_posts_path(user.id) %></td>
  </tr>
<% end %>
</table>
